# 资产管理插件 (Asset Management Plugin)

基于 DooTask 的企业资产与耗材管理系统。

## 功能特性

- 🏢 **多公司管理**: 支持多公司、多部门的层级结构
- 📦 **资产管理**: 全生命周期管理（采购、入库、领用、借用、派发、维修、报废、回收）
- 🔧 **耗材管理**: 耗材库存、领用、盘点、出库管理
- 📊 **数据分析**: 可视化仪表板、多维度统计报表
- ✅ **审批流程**: 集成 DooTask 审批和待办推送
- 📝 **盘点功能**: 资产与耗材盘点、差异分析
- 🔐 **权限控制**: 基于角色的细粒度权限管理
- 📤 **批量操作**: Excel/CSV 批量导入导出

## 技术栈

### 前端
- React 18 + TypeScript
- Vite
- Tailwind CSS
- shadcn/ui 组件库
- @dootask/tools 集成

### 后端
- Node.js + Koa
- TypeScript
- better-sqlite3 数据库
- RESTful API

### 部署
- Docker 容器化
- GitHub Actions 自动发布

## 项目结构

```
.
├── dootask-plugin/      # DooTask 插件配置
│   ├── config.yml       # 插件配置文件
│   ├── docker-compose.yml
│   └── nginx.conf       # Nginx 反向代理配置
├── ui/                  # 前端工程
│   ├── src/
│   ├── public/
│   └── package.json
├── server/              # 后端服务
│   ├── src/
│   ├── database/
│   └── package.json
├── .github/
│   └── workflows/
│       └── release.yml  # 自动发布流程
├── Dockerfile           # 容器构建配置
└── README.md
```

## 快速开始

### 前置要求

- Node.js >= 18
- Docker (生产部署)
- DooTask 主系统

### 开发环境

1. 安装依赖：

```bash
# 前端
cd ui && npm install

# 后端
cd server && npm install
```

2. 启动开发服务器：

```bash
# 后端 (端口 3000)
cd server && npm run dev

# 前端 (端口 5173)
cd ui && npm run dev
```

### 生产部署

使用 Docker 部署：

```bash
docker build -t asset-management-plugin .
docker run -d -p 8080:80 asset-management-plugin
```

或使用 DooTask 插件安装：

1. 将项目打包为 zip 文件
2. 在 DooTask 管理后台上传并安装
3. 配置插件权限和菜单

## 开发文档

详细文档请参阅 `docs/` 目录：

- 项目需求文档
- 开发计划
- API 接口文档
- 数据库设计文档

## 许可证

MIT License

## 作者

Asset Management Plugin Development Team
