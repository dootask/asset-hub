---
description: 业务领域知识 - 资产管理系统核心业务规则
---

# 业务领域知识

本文档描述资产管理系统的核心业务概念、实体关系和业务规则。详细的业务流程和状态转换已建模到 Graphiti 知识图谱中（group_id: `dootask-apps-asset-hub`），可以通过自然语言查询获取。

## 五大核心功能模块

### 1. 首页 (Home/Dashboard)

**核心职责**：提供资产概览和快捷入口

**关键功能**：
- **资产总览仪表板**
  - 实时统计：资产总额、使用中、闲置、报废、待采购
  - 数据来源：从资产表聚合计算
  - 更新频率：实时或准实时（5分钟缓存）
  
- **资产分布统计**
  - 维度：按资产类型、按部门
  - 展示方式：柱状图、饼图、趋势图
  - 使用库：ECharts 或 Recharts
  
- **快捷处理栏**
  - 审批中心：待我审批的资产操作
  - 代办事项：待我处理的任务
  - 帮助中心：系统更新通知、使用帮助

**页面路由**：`/` 或 `/home`

**数据模型**：
```typescript
interface DashboardStats {
  totalAssets: number           // 资产总数
  totalValue: number            // 资产总额
  inUseCount: number            // 使用中
  idleCount: number             // 闲置
  scrappedCount: number         // 报废
  pendingPurchaseCount: number  // 待采购
}

interface AssetDistribution {
  byType: { type: string; count: number; value: number }[]
  byDepartment: { dept: string; count: number; value: number }[]
}
```

### 2. 系统管理 (System Management)

**核心职责**：基础配置和后台管理

#### 2.1 公司管理
- **适用场景**：集团公司、连锁门店
- **操作**：添加/编辑/删除公司信息
- **字段**：公司名称、地址、联系人、联系电话
- **路由**：`/system/companies`

#### 2.2 人员管理
- **数据来源**：同步 DooTask 组织架构
- **操作**：查看、筛选、同步
- **集成**：使用 @dootask/tools 的 `fetchUserBasic` 和 `selectUsers`
- **路由**：`/system/users`

#### 2.3 角色管理
**系统角色**：
- **超级管理员**：所有权限
- **固定资产管理员**：管理固定资产（房产、车辆、设备等）
- **设备管理员**：管理IT设备（电脑、服务器等）
- **无形资产管理员**：管理无形资产（软件授权、专利等）

**审批中心配置**：
- 自定义审批流程
- 支持多级审批
- 每个资产类型可配置独立流程
- 路由：`/system/roles`、`/system/approval`

#### 2.4 数据管理
**报表统计**：
- 维度：操作人、操作类型、操作内容、时间范围
- 操作类型：入库、领用、借用、归还、派发、维修、报废
- 操作内容：新增、删除、编辑、导入、导出
- 详情查看：点击可查看操作详细记录
- 路由：`/system/reports`

**自定义报表**：
- 预留扩展功能
- 可自定义字段和格式
- 支持导出 Excel/PDF
- 路由：`/system/custom-reports`

#### 2.5 其他管理
**操作管理**：
- 定义可用的操作类型
- 配置操作所需字段
- 设置操作权限

**升级管理**：
- 版本绑定：不同版本支持不同功能
- 用户数限制：按价格/人数/年收费
- 版本有效期管理
- 续费操作
- 路由：`/system/upgrade`

### 3. 资产管理 (Asset Management)

**核心职责**：资产全生命周期管理

#### 3.1 资产设置
- 自定义资产属性字段
- 定义资产状态值
- 配置资产类型
- 路由：`/asset/settings`

#### 3.2 资产列表
**功能**：
- 列表展示（支持表格/卡片视图）
- 筛选：按状态、类型、所属人、时间范围
- 排序：按创建时间、价格、名称
- 分页：默认 20 条/页
- 搜索：支持资产名称、编号模糊搜索
- 路由：`/asset/list`

#### 3.3 资产详情
**展示内容**：
- 基本信息：名称、编号、类型、价格、购买日期
- 状态信息：当前状态、使用人、位置
- 照片：至多 10 张，支持缩略图和大图查看
- 操作历史：时间线形式展示所有操作记录
- 关联文档：合同、发票、保修卡扫描件
- 路由：`/asset/detail/:id`

#### 3.4 资产操作
**资产状态定义**：
```typescript
type AssetStatus = 
  | 'pending-purchase'  // 待采购
  | 'in-stock'          // 已入库（闲置）
  | 'in-use'            // 使用中
  | 'borrowed'          // 借出中
  | 'maintenance'       // 维修中
  | 'scrapped'          // 已报废
```

**标准操作流程**：所有操作都遵循 "提交 → 审核 → 通过" 三阶段

##### 采购 (Purchase)
- **触发**：需要新增资产
- **表单字段**：资产名称、类型、数量、预算、采购理由、供应商
- **审批**：根据预算金额决定审批级别
- **状态变更**：无 → `pending-purchase`
- **路由**：`/asset/purchase`

##### 入库 (Receive/Stock-in)
- **触发**：采购完成或资产归还
- **表单字段**：资产信息、实际价格、购买日期、发票号
- **照片上传**：至少 3 张不同角度照片（可选但推荐）
- **审批**：入库审核
- **状态变更**：`pending-purchase` → `in-stock`
- **路由**：`/asset/receive`

##### 领用 (Claim/Assign)
- **触发**：员工需要使用资产
- **表单字段**：领用人、领用原因、预计归还时间
- **审批**：部门主管或资产管理员
- **状态变更**：`in-stock` → `in-use`
- **记录**：记录领用人和领用时间
- **路由**：`/asset/claim`

##### 借用/归还 (Borrow/Return)
- **借用流程**：
  - 表单：借用人、借用原因、预计归还时间、借用地点
  - 审批：资产所有人或管理员
  - 状态变更：`in-stock` 或 `in-use` → `borrowed`
  - 逾期提醒：到期前1天提醒，逾期后每天提醒
  - 提醒方式：DooTask 待办事项（集成 `openTask` API）

- **归还流程**：
  - 表单：归还确认、资产状态检查
  - 审批：确认资产完好
  - 状态变更：`borrowed` → `in-stock` 或 `in-use`
  
- **路由**：`/asset/borrow`, `/asset/return`

##### 派发 (Transfer/Dispatch)
- **用途**：跨部门、跨公司资产调拨
- **表单字段**：
  - 接收方信息：部门/公司、接收人
  - 发货信息：发货数量、日期、经办人
  - 物流信息：物流公司、运单号、运费
- **审批**：双方管理员确认
- **状态变更**：记录新的所属人和位置
- **路由**：`/asset/transfer`

##### 其他操作
**维修 (Maintenance)**：
- 表单：故障描述、维修地点、预计费用
- 审批：费用审批
- 状态变更：原状态 → `maintenance` → 原状态
- 路由：`/asset/maintenance`

**报废 (Scrap)**：
- 表单：报废原因、评估价值、处理方式
- 审批：资产管理员和财务
- 状态变更：任意状态 → `scrapped`
- 特殊处理：报废后不可再使用，但保留历史记录
- 路由：`/asset/scrap`

**回收 (Recycle)**：
- 用途：将已派发的资产收回
- 流程：类似归还
- 路由：`/asset/recycle`

**遗失 (Lost)**：
- 表单：遗失时间、地点、经过、责任人
- 审批：多级审批（涉及责任认定）
- 处理：记录遗失，财务处理
- 路由：`/asset/lost`

#### 3.5 资产盘点 (Inventory)
**创建盘点任务**：
- 设置：盘点名称、盘点范围（资产类型）、盘点时间、负责人
- 分配：自动生成盘点清单
- 执行：负责人逐一核对
- 结果：盘盈、盘亏、正常
- 导出：盘点报告下载（Excel）
- 路由：`/asset/inventory`

#### 3.6 分类管理
**资产类型示例**：
- 固定资产：房产、车辆、大型设备
- IT 设备：电脑、服务器、网络设备
- 办公用品：办公桌椅、文具
- 无形资产：软件授权、域名、专利

**自定义类型**：
- 类型名称
- 类型代码（用于编号规则）
- 默认审批流程
- 自定义字段模板
- 路由：`/asset/categories`

### 4. 耗材管理 (Consumable Management)

**状态**：后期扩展功能

**核心职责**：易耗品的管理

**与资产管理的区别**：
- 资产：唯一性，可追踪到个体
- 耗材：批次管理，追踪数量而非个体

**主要功能**：
- 耗材设置：类别、规格、最低库存预警
- 耗材操作：采购、入库、领用/出库、处理
- 出库管理：一般在月底盘点后统一出库
- 耗材处理：过期、损坏、遗失
- 耗材盘点：定期盘点库存
- 路由：`/consumable/*`

**数据模型**：
```typescript
interface Consumable {
  id: number
  name: string
  category: string
  specification: string  // 规格型号
  unit: string           // 单位（个、盒、包）
  quantity: number       // 库存数量
  minQuantity: number    // 最低库存预警
  price: number          // 单价
  location: string       // 存放位置
}
```

### 5. 版本号展示

**需求**：在系统主界面或个人中心突出显示当前版本号

**实现位置**：
- 页面底部 Footer
- 系统设置页面
- 关于页面

**显示格式**：`v1.0.0` 或 `资产管理 v1.0.0`

**获取方式**：
```typescript
// 从 package.json 读取
import packageJson from '../package.json'
const version = packageJson.version
```

## 通用业务规则

### 审批流程规则

1. **三阶段流程**：所有需要审批的操作都遵循：
   - 提交（pending）
   - 审核中（reviewing）
   - 通过/拒绝（approved/rejected）

2. **多级审批**：
   - 可配置 1-5 级审批
   - 审批人可以是：具体用户、角色、部门主管
   - 审批方式：依次审批或会签

3. **审批意见**：
   - 每级审批必须填写意见
   - 拒绝时必须说明理由
   - 审批历史完整记录

4. **审批通知**：
   - 提交后通知审批人（DooTask 待办）
   - 审批通过/拒绝通知申请人
   - 逾期未审批提醒

### 权限控制规则

1. **操作权限**：
   - 超级管理员：所有权限
   - 资产类型管理员：对应类型的增删改查和审批
   - 普通用户：只能查看自己的资产和提交申请

2. **数据权限**：
   - 公司级：只能看本公司数据
   - 部门级：只能看本部门数据
   - 个人级：只能看自己相关数据

3. **审批权限**：
   - 按角色配置审批权限
   - 按金额配置审批级别
   - 支持委托审批

### 数据验证规则

1. **资产编号**：
   - 格式：`{类型代码}-{年份}{月份}-{流水号}`
   - 示例：`FA-202501-001` (固定资产)
   - 唯一性：全局唯一

2. **日期规则**：
   - 购买日期 ≤ 入库日期
   - 借用开始时间 ≤ 归还时间
   - 不能选择未来日期（除预计归还时间）

3. **金额规则**：
   - 价格必须 > 0
   - 支持两位小数
   - 总额自动计算

4. **状态转换规则**：
   - 有些状态转换是不允许的
   - 例如：`scrapped` 不能转为其他状态
   - 必须通过特定操作才能改变状态

### 提醒规则

1. **借用逾期提醒**：
   - 到期前 1 天提醒借用人和资产所有人
   - 逾期后每天提醒，直到归还
   - 逾期 7 天上报管理员

2. **库存预警**：
   - 耗材低于最低库存时提醒采购
   - 预警频率：每周一次

3. **盘点提醒**：
   - 盘点开始时通知负责人
   - 盘点到期前 2 天提醒
   - 逾期未完成上报管理员

## 数据字典

### 资产状态
| 值 | 中文 | 英文 | 说明 |
|----|------|------|------|
| `pending-purchase` | 待采购 | Pending Purchase | 已申请但未购买 |
| `in-stock` | 已入库 | In Stock | 闲置状态，可领用 |
| `in-use` | 使用中 | In Use | 已分配给用户 |
| `borrowed` | 借出中 | Borrowed | 临时借出 |
| `maintenance` | 维修中 | Maintenance | 正在维修 |
| `scrapped` | 已报废 | Scrapped | 终态，不可再用 |

### 操作类型
| 值 | 中文 | 英文 | 需要审批 |
|----|------|------|---------|
| `purchase` | 采购 | Purchase | ✅ |
| `receive` | 入库 | Receive | ✅ |
| `claim` | 领用 | Claim | ✅ |
| `borrow` | 借用 | Borrow | ✅ |
| `return` | 归还 | Return | ✅ |
| `transfer` | 派发 | Transfer | ✅ |
| `maintenance` | 维修 | Maintenance | ✅ |
| `scrap` | 报废 | Scrap | ✅ |
| `recycle` | 回收 | Recycle | ✅ |
| `lost` | 遗失 | Lost | ✅ |

### 审批状态
| 值 | 中文 | 英文 |
|----|------|------|
| `pending` | 待审批 | Pending |
| `reviewing` | 审批中 | Reviewing |
| `approved` | 已通过 | Approved |
| `rejected` | 已拒绝 | Rejected |

## 与 Graphiti 的协作

本文档提供静态的业务概念和规则定义。对于以下动态查询，请使用 Graphiti：

- "某个资产的完整操作历史是什么？"
- "借用资产的完整审批流程是怎样的？"
- "固定资产管理员有哪些权限？"
- "资产从待采购到报废的完整生命周期？"
- "哪些操作会触发状态变更？"

使用 Graphiti 的 `search_memory_facts` 工具进行自然语言查询。

## 开发提示

1. **模块化开发**：每个功能模块独立开发，通过路由组织
2. **组件复用**：审批流程、状态展示、操作历史等可封装为通用组件
3. **状态管理**：使用 Zustand 管理当前用户、权限、筛选条件等全局状态
4. **API 设计**：RESTful 风格，统一的请求/响应格式
5. **权限检查**：前后端都要做权限校验
6. **日志记录**：所有重要操作都要记录操作日志

## 未来扩展方向

1. **移动端优化**：适配手机和平板，支持扫码盘点
2. **物联网集成**：RFID 标签、位置追踪
3. **AI 辅助**：资产价值评估、采购建议、异常检测
4. **报表增强**：更多维度的数据分析和可视化
5. **对接ERP**：与企业 ERP 系统集成
